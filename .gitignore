# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
.terraform.lock.hcl

# Keep example/non-secret tfvars, ignore anything explicitly local/secret
*.auto.tfvars
!terraform/environments/*/terraform.tfvars
*.secret.tfvars

# Ansible
ansible/inventories/*/hosts.yml
!ansible/inventories/*/hosts.yml.example
*.retry
ansible/**/*.vault
.vault_pass*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
*.log

# Reports / generated artifacts (root-level only — anchored with a
# leading slash so this doesn't also match the real source package at
# python/cloudforge/reports/)
/reports/
*.report.json
*.report.html

# Secrets / credentials (defense in depth - none should ever be committed)
*.pem
*.key
.env
.env.*
credentials.json
secrets.yml
secrets.yaml

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
